version: '3.8'
services:
  accountant:
    volumes:
      - "/preferences-dev.yml:/preferences.yml"
  matching-engine:
    volumes:
      - "/preferences-dev.yml:/preferences.yml"
  wallet:
    volumes:
      - "/preferences-dev.yml:/preferences.yml"
  api:
    volumes:
      - "/preferences-dev.yml:/preferences.yml"
  bc-gateway:
    volumes:
      - "/preferences-dev.yml:/preferences.yml"
