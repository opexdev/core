package co.nilin.opex.common.data
//After adjusting versions in various modules it should be moved to common project

enum class MessageCategory {
    CURRENCY, ERROR_CODE, CONTACT, KYC_PARAM, DEFAULT
}